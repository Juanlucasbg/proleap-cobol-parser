{
  "name": "saleads-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "Environment-agnostic Playwright E2E tests for SaleADS.ai workflows.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0"
  }
}
