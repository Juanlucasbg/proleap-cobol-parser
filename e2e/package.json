{
  "name": "saleads-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright workflow tests for SaleADS.ai",
  "scripts": {
    "test": "playwright test",
    "test:workflow": "playwright test tests/saleads-mi-negocio-full.spec.js",
    "test:headed": "playwright test --headed",
    "codegen": "playwright codegen"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.0"
  }
}
