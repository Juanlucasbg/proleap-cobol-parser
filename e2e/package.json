{
  "name": "saleads-e2e",
  "private": true,
  "version": "1.0.0",
  "description": "SaleADS end-to-end tests for Mi Negocio workflow",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0"
  }
}
