{
  "name": "saleads-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "Environment-agnostic SaleADS E2E workflows",
  "scripts": {
    "test": "playwright test",
    "test:saleads:mi-negocio": "playwright test tests/saleads_mi_negocio_full_test.spec.ts",
    "test:headed": "playwright test --headed",
    "install:browsers": "playwright install --with-deps chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.1",
    "@types/node": "^24.3.0",
    "typescript": "^5.9.2"
  }
}
