{
  "name": "saleads-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "Standalone Playwright E2E tests for SaleADS workflows",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "playwright": "^1.59.1"
  }
}
