{
  "name": "saleads-e2e",
  "private": true,
  "version": "1.0.0",
  "description": "End-to-end tests for SaleADS workflows.",
  "scripts": {
    "test": "playwright test",
    "test:saleads-mi-negocio": "playwright test tests/saleads_mi_negocio_full_test.spec.js",
    "test:headed": "playwright test --headed",
    "install:browsers": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.2"
  }
}
