{
  "name": "saleads-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright E2E checks for SaleADS workflows",
  "scripts": {
    "test": "playwright test",
    "test:mi-negocio": "playwright test tests/saleads_mi_negocio_full_test.spec.ts",
    "test:headed": "playwright test --headed",
    "install:browsers": "playwright install --with-deps chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0"
  }
}
