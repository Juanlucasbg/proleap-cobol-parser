{
  "name": "e2e",
  "version": "1.0.0",
  "description": "SaleADS end-to-end automation tests",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:smoke": "playwright test --list",
    "install:browsers": "playwright install chromium"
  },
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.59.1"
  }
}
