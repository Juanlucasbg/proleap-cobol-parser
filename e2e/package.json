{
  "name": "saleads-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright E2E tests for SaleADS workflows",
  "scripts": {
    "test": "mkdir -p artifacts && playwright test",
    "test:saleads-mi-negocio": "mkdir -p artifacts && playwright test tests/saleads-mi-negocio.spec.js --project=chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0"
  }
}
