{
  "name": "saleads-mi-negocio",
  "version": "1.0.0",
  "description": "Environment-agnostic Playwright workflow test for SaleADS Mi Negocio",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "install:browsers": "playwright install --with-deps chromium"
  },
  "keywords": [
    "playwright",
    "e2e",
    "saleads"
  ],
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.61.1"
  }
}
