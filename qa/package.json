{
  "name": "saleads-qa-e2e",
  "private": true,
  "version": "1.0.0",
  "description": "Environment-agnostic SaleADS UI workflow checks",
  "scripts": {
    "test": "playwright test",
    "test:saleads-mi-negocio": "playwright test saleads-mi-negocio.spec.js",
    "install:browsers": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "latest"
  }
}
