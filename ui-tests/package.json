{
  "name": "saleads-ui-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Portable Playwright workflow tests for SaleADS.ai",
  "scripts": {
    "test": "playwright test",
    "test:saleads-mi-negocio": "playwright test tests/saleads-mi-negocio-full.spec.ts",
    "test:headed": "playwright test --headed",
    "install:browsers": "playwright install"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.1",
    "@types/node": "^24.0.3"
  }
}
