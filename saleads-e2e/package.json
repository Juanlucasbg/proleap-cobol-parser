{
  "name": "saleads-e2e",
  "private": true,
  "version": "1.0.0",
  "description": "SaleADS end-to-end browser automation",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "test:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0"
  }
}
