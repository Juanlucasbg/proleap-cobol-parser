{
  "name": "saleads-e2e-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Environment-agnostic SaleADS.ai end-to-end workflow tests",
  "scripts": {
    "test": "playwright test",
    "test:list": "playwright test --list",
    "test:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0"
  }
}
