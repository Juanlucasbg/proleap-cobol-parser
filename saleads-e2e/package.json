{
  "name": "saleads-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "Cross-environment Playwright E2E tests for SaleADS.ai",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:debug": "playwright test --debug"
  },
  "devDependencies": {
    "@playwright/test": "latest"
  }
}
