{
  "name": "saleads-e2e-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Environment-agnostic SaleADS UI workflow tests.",
  "scripts": {
    "test:e2e": "playwright test",
    "test:e2e:headed": "playwright test --headed",
    "test:e2e:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0"
  }
}
